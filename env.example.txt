# Postgres — na Railway isso é preenchido automaticamente quando você
# conecta um serviço PostgreSQL ao projeto (variável ${{Postgres.DATABASE_URL}}).
DATABASE_URL="postgresql://user:password@localhost:5432/dock_trust_platform"

PORT=3333

# Segredo usado para assinar os tokens JWT — GERE UM VALOR PRÓPRIO, nunca
# reaproveite este. Sugestão pra gerar um bom valor:
#   node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET="troque-por-um-valor-aleatorio-longo"

# Domínios que podem chamar esta API, separados por vírgula (sem espaço
# depois da vírgula é opcional, o código já remove espaços extras).
CORS_ALLOWED_ORIGINS="https://docktrust.co,https://www.docktrust.co,http://localhost:5173"

# "production" desativa o retorno do código OTP na resposta da API (ver
# routes/auth.ts) — sempre "production" no deploy real.
NODE_ENV="development"

# Senha inicial da conta administradora (admin@docktrust.co), usada só na
# primeira vez que o seed roda (ou quando renomeia uma conta antiga). O
# script FALHA se essa variável não estiver definida — é proposital, senão
# a senha teria que ficar escrita no código. Escolha algo forte e troque a
# senha pelo próprio painel depois do primeiro login.
INITIAL_ADMIN_PASSWORD="troque-por-uma-senha-forte"

